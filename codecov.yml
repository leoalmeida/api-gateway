codecov:
  require_ci_to_pass: false

coverage:
  precision: 2
  round: down
  range: 50..90
  status:
    project:
      default:
        target: auto
        threshold: 1%
      api-gateway:
        flags:
          - api-gateway

flags:
  api-gateway:
    paths:
      - src
